Syntax:
if e1 then e2 then e3
		       where if, then, and else are keywords and
		   e1, e2, and e3 are subexpressions

				  Type-Checking:
			       firstr e1 must have type bool
							    e2 and e3 can have any type (let's call it t), but they m




													       